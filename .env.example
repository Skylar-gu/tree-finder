# Copy to .env and adjust. docker-compose reads these.
POSTGRES_USER=trees
POSTGRES_PASSWORD=trees
POSTGRES_DB=trees
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# API
API_HOST=0.0.0.0
API_PORT=8000

# Optional: Mapillary client token for Tier C imagery (NOT wired in v1 — slot only).
MAPILLARY_TOKEN=
